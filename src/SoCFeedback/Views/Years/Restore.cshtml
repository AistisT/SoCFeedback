@model Year

@{
    ViewData["Title"] = "Restore Year Form";
}

<h2>Archive</h2>

<h3 class="text-danger">This will restore archived year form back to a pending state, this action will automatically archive any currently published or pending year forms.</h3>
<hr/>
<div>
    <h4>Year Form</h4>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Year1)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AcademicYear)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Status)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Status)
        </dd>
    </dl>
    <div class="row">
        <div class="col-md-12">
            <hr />
            <h4>Modules</h4>
            <hr />
            @for (var j = 0; j < Model.Levels.Count; j++)
            {
                <span class=" text-primary h4 form-text">
                    @Html.DisplayFor(m => m.Levels[j].Title)
                </span>
                <ul class="list-group">
                    @for (var i = 0; i < Model.Modules.Count; i++)
                {
                    if (Model.Modules[i].LevelId == Model.Levels[j].Id)
                    {
                            <li class=" list-group-item col-md-12">

                                @Html.DisplayFor(m => m.Modules[i].Code) @Html.DisplayFor(m => m.Modules[i].Title)
                            </li>
                        }
                    }
                </ul>
            }
        </div>
    </div>
    <form asp-action="Restore"asp-route-id="@Model.Id">
        <div class="form-group">
            <br/>
            <input type="submit" value="Restore" class="btn btn-primary"/>
            <a asp-action="Index" class="float-xs-right">Back to List</a>
        </div>
    </form>
</div>