namespace SoCFeedback.Enums
{
    public enum ModuleStatus
    {
        /// <summary>
        /// Module this running this year
        /// </summary>
        Running = 0,

        /// <summary>
        /// Module is not running this year for w/e reason, thus paused, but not archived.
        /// </summary>
        Paused = 1,
    }
}